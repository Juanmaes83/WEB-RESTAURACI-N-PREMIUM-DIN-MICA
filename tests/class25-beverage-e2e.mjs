/* CLASS 25 — BEVERAGE EXPERIENCE · Playwright visual/integration gate
   Proves the feature on the CURRENT Half Orbit product line, not a reconstructed page. */
import {chromium} from 'playwright';
import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {startServer} from './static-server.mjs';
const ROOT=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const OUT=path.join(ROOT,'tests','screenshots','class25-beverages');fs.mkdirSync(OUT,{recursive:true});
const target=process.argv[2],local=target?null:await startServer(0),BASE=(target||local.url).replace(/\/$/,'');
const browser=await chromium.launch();const checks=[];const check=(n,ok,d='')=>{checks.push(ok);console.log(`${ok?'PASS':'FAIL'} ${n}${d?` — ${d}`:''}`)};
async function boot(page){const errors=[];page.on('pageerror',e=>errors.push(e.message));await page.goto(`${BASE}/?review=beverages`,{waitUntil:'domcontentloaded',timeout:45000});await page.waitForFunction(()=>window.RestaurantBeveragesEngine?.state?.().ready===true,null,{timeout:40000});await page.waitForTimeout(700);return errors}
{
 const ctx=await browser.newContext({viewport:{width:1440,height:900}}),page=await ctx.newPage(),errors=await boot(page);
 const initial=await page.evaluate(()=>{const h=document.querySelector('#beverages'),img=h?.querySelector('.bev-media');return {headers:document.querySelectorAll('header.topbar').length,studios:document.querySelectorAll('#studio').length,beverages:!!h,title:h?.querySelector('.bev-title')?.textContent.trim(),active:h?.dataset.activeItem,imgSrc:img?.getAttribute('src')||'',imgReady:!!img&&img.complete&&img.naturalWidth>0,halfOrbit:!!window.RestaurantHalfOrbit,motionGroups:window.RestaurantMotionGovernance?.state?.().groups||[],libraryCount:window.RestaurantMotionLibrary?.count?.()||0,bevTab:!!document.querySelector('#studio .studio-nav [data-panel="beverages"]'),motionTab:!!document.querySelector('#studio .studio-nav [data-panel="motion"]')};});
 check('latest line · exactly one original top navigation',initial.headers===1,String(initial.headers));check('latest line · exactly one Restaurant Studio',initial.studios===1,String(initial.studios));check('Beverage Experience mounts in real page',initial.beverages&&initial.title==='Una bebida. Un mundo.',initial.title);check('repo-local ASIATICO asset paints',initial.imgReady&&/ASIATICO(?:%20| )1\.png/i.test(initial.imgSrc),initial.imgSrc);check('Half Orbit runtime is still present',initial.halfOrbit);check('current Motion Governance remains present',initial.motionGroups.includes('page')&&initial.motionGroups.includes('preset'),initial.motionGroups.join(','));check('Motion Library still has 12 existing engines',initial.libraryCount===12,String(initial.libraryCount));check('Bebidas and Motion are separate Studio tabs',initial.bevTab&&initial.motionTab);
 await page.locator('#beverages').screenshot({path:path.join(OUT,'01-beverage-desktop.png')});
 const before=await page.evaluate(()=>({id:document.querySelector('#beverages').dataset.activeItem,bg:getComputedStyle(document.querySelector('#beverages')).getPropertyValue('--bev-backdrop')}));await page.click('.bev-option[data-index="1"]');await page.waitForFunction(id=>document.querySelector('#beverages')?.dataset.activeItem!==id,before.id,{timeout:5000});await page.waitForTimeout(1300);const after=await page.evaluate(()=>({id:document.querySelector('#beverages').dataset.activeItem,bg:getComputedStyle(document.querySelector('#beverages')).getPropertyValue('--bev-backdrop'),transition:window.RestaurantBeveragesEngine.state().transition}));check('selector changes product',after.id!==before.id,`${before.id} -> ${after.id}`);check('selector changes full visual world',after.bg!==before.bg);check('transition settles',after.transition==='settled',after.transition);
 await page.click('.studio-open');await page.waitForTimeout(350);await page.evaluate(()=>window.RestaurantBeveragesStudio.open());await page.waitForTimeout(300);const studio=await page.evaluate(()=>({open:document.querySelector('#studio')?.classList.contains('is-open'),active:document.querySelector('#studio .studio-nav button.active')?.dataset.panel,panels:document.querySelectorAll('#studio').length,motionTabs:document.querySelectorAll('#studio .studio-nav [data-panel="motion"]').length,travelerTuner:document.querySelectorAll('.st-studio-governed').length}));check('Bebidas opens inside SAME Studio',studio.open&&studio.active==='beverages'&&studio.panels===1,JSON.stringify(studio));check('Traveler remains separate in current Motion Studio',studio.motionTabs===1&&studio.travelerTuner===1,JSON.stringify(studio));await page.screenshot({path:path.join(OUT,'02-beverage-studio.png'),fullPage:false});check('desktop · no JS page errors',errors.length===0,errors.join(' | '));await ctx.close();
}
{
 const ctx=await browser.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true}),page=await ctx.newPage(),errors=await boot(page);const m=await page.evaluate(()=>({scrollWidth:document.documentElement.scrollWidth,width:innerWidth,rect:(()=>{const r=document.querySelector('#beverages')?.getBoundingClientRect();return r&&{left:r.left,right:r.right,width:r.width}})(),img:(()=>{const i=document.querySelector('.bev-media');return !!i&&i.complete&&i.naturalWidth>0})()}));check('mobile 390 · no horizontal overflow',m.scrollWidth<=m.width,`${m.scrollWidth}/${m.width}`);check('mobile · beverage asset paints',m.img);await page.locator('#beverages').screenshot({path:path.join(OUT,'03-beverage-mobile.png')});check('mobile · no JS page errors',errors.length===0,errors.join(' | '));await ctx.close();
}
{
 const ctx=await browser.newContext({viewport:{width:1440,height:900},reducedMotion:'reduce'}),page=await ctx.newPage();await boot(page);const first=await page.evaluate(()=>document.querySelector('#beverages').dataset.activeItem);await page.click('.bev-option[data-index="1"]');await page.waitForTimeout(100);const second=await page.evaluate(()=>document.querySelector('#beverages').dataset.activeItem);check('reduced motion · selector remains functional',first!==second,`${first} -> ${second}`);await ctx.close();
}
await browser.close();if(local)await new Promise(r=>local.server.close(r));const passed=checks.filter(Boolean).length;console.log(`\n${passed}/${checks.length} ${passed===checks.length?'CLASS25_BEVERAGE_PASS':'CLASS25_BEVERAGE_FAIL'}`);if(passed!==checks.length)process.exitCode=1;