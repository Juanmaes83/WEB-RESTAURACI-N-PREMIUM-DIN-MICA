const {test}=require('node:test');const assert=require('node:assert/strict');const media=require('../rubik-seo-geo-media.js');
const base=()=>({brand:{name:'Casa'},dishes:[{id:'dish-1',name:'Gamba roja'}],media:{'dish-1':{originalName:'Gamba Roja.JPG',type:'image',width:1200,height:900},hero:{type:'image',loading:'lazy'}},seo:{media:{}}});
test('B1 converges the productive dish image source',()=>{const c=base();delete c.media['dish-1'];c.dishes[0].image='https://cdn.example.test/gamba-original.jpg';const a=media.project(c,'dish-1');assert.equal(a.originalName,'https://cdn.example.test/gamba-original.jpg');assert.equal(a.alt.value,'Gamba roja');});
test('B1 AUTO/CUSTOM filename and factual ALT',()=>{const c=base(),a=media.project(c,'dish-1','2026-01-01');assert.equal(a.seoFilename.value,'gamba-roja');assert.equal(a.alt.value,'Gamba roja');c.seo.media={'dish-1':{seoFilename:{mode:'custom',value:'plato-especial',updatedAt:'x'}}};assert.equal(media.project(c,'dish-1').seoFilename.value,'plato-especial');});
test('decorative media has empty ALT and unknown context does not hallucinate',()=>{const c=base();c.media.hero.decorative=true;assert.equal(media.project(c,'hero').alt.value,'');const unknown={media:{x:{type:'image'}},seo:{media:{}}};assert.equal(media.project(unknown,'x').alt.value,'');});
test('audit warns dimensions and hero lazy; VideoObject only with complete editorial metadata',()=>{const c=base(),issues=media.audit(c);assert.ok(issues.some(x=>x.id==='media.hero.dimensions'));assert.ok(issues.some(x=>x.id==='media.hero.lcp'));assert.equal(media.videoObject(c,'hero'),null);c.media.v={type:'video',isSeoContent:true,title:'Cocina',description:'Proceso real',thumbnailRef:'/thumb.jpg',contentUrl:'/video.mp4'};assert.equal(media.videoObject(c,'v')['@type'],'VideoObject');});
