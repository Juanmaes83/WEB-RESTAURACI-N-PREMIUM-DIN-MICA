# Rubik SEO/GEO Core — Release A · Search-Ready Core

**Status:** Canonical delivery contract for the active implementation in PR #44.

This document is an additive canonical overlay for the current branch. It exists to lock the revised execution model without rewriting files Astra may already be editing locally. Where this document conflicts with the old Phase 1 / Phase 2 operational split in `SEO-GEO-ENGINE-ARCHITECTURE.md` or `SEO-GEO-FOUNDATION.md`, **this Release A contract governs the active PR #44**. After Release A is approved and merged, the broader architecture document must be consolidated to remove obsolete wording.

---

## 1. Product objective

Rubik SEO/GEO Core must make every site generated by the platform **search-ready by default**, with the highest possible degree of safe automation from real client data.

Canonical flow:

```text
BUSINESS DATA
→ PROJECT STATE
→ SEO/GEO DECISION ENGINE
→ METADATA + SCHEMA + CONTENT + MEDIA + BLOG + LOCAL SEO
→ PUBLISH
→ AUDIT + REAL MEASUREMENT
```

Release A covers the first production-ready slice:

```text
BUSINESS DATA
→ PROJECT STATE
→ SENIOR SEO DECISION ENGINE
→ HOME SEO
→ PRODUCTIVE PUBLISHER
→ PRE-PUBLISH AUDIT
```

No second Store, CMS, database, Media Library or parallel application may be introduced.

---

## 2. Operational roadmap — 4 Releases

The previous Phase 0–9 map remains useful as technical traceability, but execution is regrouped into four product Releases.

### Release A — Search-Ready Core

Combines the old Foundation + Publisher scope:

- inheritance from existing Project State;
- Business/Entity model;
- HOME Page Registry;
- AUTO/CUSTOM;
- Senior SEO Decision Engine;
- title, meta description and H1 recommendation;
- senior pre-publish audit;
- productive metadata publishing;
- canonical;
- robots/indexability;
- Open Graph / Twitter metadata;
- productive JSON-LD;
- sitemap;
- preview/production policy;
- raw HTML validation.

### Release B — Content & Media

Regroups Media SEO + Multi-page Page Registry + Blog Engine:

- image filename / ALT / caption;
- video metadata / VideoObject when justified;
- performance warnings;
- real URLs for carta, chef, reservas, ubicación and other valid intents;
- breadcrumbs / internal linking;
- Blog Engine;
- H1/H2/H3 structure;
- Article/BlogPosting;
- author/entity relationships;
- publishing.

### Release C — Intelligence & GEO

Regroups Audit & Measurement + Keyword Intelligence + GEO / AI Search:

- OpenSEO;
- `seo-god` workflow adaptation;
- Search Console;
- snapshots / regressions / quick wins;
- DataForSEO optional power-up;
- real keyword metrics / SERPs / competitors / gaps;
- crawler controls;
- AI Search readiness;
- entity consistency;
- citability heuristic;
- honest AI reporting;
- `llms.txt` only as experimental interoperability.

### Release D — Multi-vertical Rubik Core

- stable reusable Core API;
- Restaurant Adapter complete;
- Real Estate Adapter;
- Personal Trainer Adapter;
- Generic Local Business Adapter;
- reuse across Rubik products.

---

## 3. Spain-first contract

Release A supports **Spanish SEO only**.

Canonical runtime configuration:

```text
language = es
locale = es-ES
supportedLanguages = [es]
```

The current English selector / pseudo-bilingual formulas must be removed or disabled from the operational Studio UI.

### Technical debt

Register explicitly:

```text
TECH DEBT · MULTILINGUAL SEO
```

Future multilingual support must mean real localized content, not string substitution. It requires:

- content by locale;
- metadata by locale;
- Page Registry by locale;
- canonical by locale;
- hreflang only for real equivalent URLs;
- schema/inLanguage by locale;
- AUTO/CUSTOM state by locale;
- human-reviewed translation/localization workflow.

Do not simulate English by translating only `Restaurante → Restaurant` or `en → in`.

---

## 4. SEO quality standard

The system is not a static template filler. It acts as a deterministic **Senior SEO Decision Engine**.

Required characteristics:

- deterministic;
- versioned;
- explainable;
- testable;
- data-driven;
- no invented facts;
- no keyword stuffing;
- no fake ranking / AI visibility claims.

Decision flow:

```text
SOURCE DATA
→ generate valid candidates
→ apply hard rules
→ evaluate quality
→ select best candidate
→ store provenance
```

Every AUTO generated field must preserve at least:

```js
{
  mode: 'auto',
  value: '',
  templateId: '',
  derivedFrom: [],
  decisionReason: '',
  updatedAt: ''
}
```

CUSTOM always overrides AUTO. Returning to AUTO recalculates from current Source Data.

---

## 5. Hard rules

Never invent or infer silently:

- city;
- cuisine;
- category specifics;
- chef;
- dishes;
- opening hours;
- coordinates;
- prices;
- reviews;
- ratings;
- services;
- reservation capability;
- CTA;
- claims.

Never use phone/email in default title or H1.

Never generate `meta keywords`.

Avoid:

- keyword stuffing;
- repetitive boilerplate;
- duplicated concepts;
- thin doorway-style pages;
- fake local modifiers;
- Vercel preview URLs as canonical.

---

## 6. HOME title decision model

The approved formula is a candidate, not a mandatory literal string.

Candidate family may include:

```text
{{brand}} | {{category}} en {{city}}
{{category}} en {{city}} | {{brand}}
{{brand}} | {{primaryCuisineOrService}} en {{city}}
{{brand}} | {{category}}
```

The engine selects the best valid candidate using, in order:

1. actual HOME intent;
2. specificity;
3. real local signal when city exists;
4. real category/cuisine/service data;
5. natural Spanish;
6. absence of redundancy;
7. consistency with visible content/H1;
8. branding.

Length is advisory only, never a rigid Google limit.

Fallbacks must degrade gracefully when data is incomplete.

---

## 7. HOME meta description — canonical composition strategy

The old simplified implementation (`brand + category + hero.body`) is insufficient.

The canonical composition strategy is:

```text
BRAND
+ WHAT IT IS
+ SPECIALITY / PRIMARY SERVICE
+ LOCATION
+ REAL VALUE PROPOSITION
+ REAL CTA
```

Each component is conditional and must only appear when supported by Source Data.

Full example:

```text
LÚMINA es un restaurante gastronómico de cocina mediterránea contemporánea en Alicante. Descubre una carta de producto y temporada y reserva tu mesa.
```

Rules:

- `reserva tu mesa` only if a real reservation capability/URL exists;
- cuisine only if known;
- locality only if known;
- offer/value proposition only if present in real source content;
- no synthetic claims;
- natural Spanish, not mechanical concatenation;
- if data is incomplete, generate a truthful reduced version and raise an audit opportunity/warning.

---

## 8. H1 contract

The engine may recommend the best H1, but must not create hidden duplicate H1s or silently overwrite approved editorial copy.

Rules:

- detect the existing visible H1;
- keep exactly one principal visible H1 as the product editorial rule;
- align recommendation and public H1 explicitly;
- never inject a second hidden H1 for SEO;
- if human copy remains CUSTOM, preserve it and audit consistency instead of silently replacing it.

---

## 9. Senior pre-publish audit

A string existing is not equivalent to SEO quality.

Every relevant field is evaluated across:

```text
EXISTENCE
COMPLETENESS
SPECIFICITY
CONSISTENCY
PUBLISHABILITY
```

Canonical severities:

- `BLOCKER` — prevents safe/valid production SEO or may break indexation;
- `ERROR` — serious issue in active Release scope;
- `WARNING` — important quality issue;
- `OPPORTUNITY` — non-mandatory improvement;
- `PASS` — valid and sufficiently complete for the evaluated contract;
- `DEFERRED` — intentionally belongs to a later Release and must not reduce Release A readiness.

Examples:

- generic fallback category only → WARNING or OPPORTUNITY;
- local business without city → WARNING;
- no cuisine/service differentiator → OPPORTUNITY;
- generic title despite richer source data → WARNING;
- title/H1 contradiction → WARNING;
- invented factual data → ERROR/BLOCKER;
- missing productive canonical at publish time → BLOCKER;
- OpenSEO/Search Console/DataForSEO not yet implemented → DEFERRED, not BLOCKER.

No fake score such as `Google Rank Score` or probability of ChatGPT citation.

---

## 10. Release A Publisher contract

Release A must publish the SEO, not merely preview it.

Required productive outputs:

```html
<title>...</title>
<meta name="description" content="...">
<link rel="canonical" href="...">
<meta name="robots" content="...">
<meta property="og:type" content="...">
<meta property="og:title" content="...">
<meta property="og:description" content="...">
<meta property="og:url" content="...">
<meta property="og:image" content="...">
<meta name="twitter:card" content="summary_large_image">
```

Plus productive JSON-LD and generated sitemap/robots policy.

### Critical technical requirement

Productive metadata must exist in the **initial served/published HTML output**. It must not depend exclusively on client-side JavaScript mutating `document.head` after load.

Acceptance must include raw HTML validation via HTTP/fetch/curl or equivalent with JavaScript disabled/not executed.

---

## 11. Productive schema contract

For Restaurant HOME, emit a coherent graph where real data exists:

```text
WebSite
→ WebPage
→ Restaurant
```

Optional nodes/properties only from real public data:

- PostalAddress;
- GeoCoordinates;
- OpeningHoursSpecification;
- image;
- servesCuisine;
- telephone;
- email;
- priceRange;
- menu;
- reservation capability/URL;
- sameAs;
- Person/chef only when real and relevant.

Never generate fake `aggregateRating`, reviews, people, hours or coordinates.

Structured data must agree with visible/public content.

---

## 12. Canonical, robots and environment policy

Rules:

- Vercel preview URL is never canonical;
- productive canonical derives from valid `seo.site.baseUrl` + Page Registry;
- missing valid productive base URL at publish time = BLOCKER;
- preview/staging = noindex/protected policy;
- production = index/noindex according to Page Registry;
- preview noindex must never leak accidentally into production;
- production index policy must never make preview indexable.

---

## 13. Legacy SEO migration

Release A must replace obsolete/demo metadata and schema without duplicates.

Productive gate:

```text
1 effective title
1 meta description
1 canonical when applicable
coherent robots policy
1 coherent productive JSON-LD graph/set
0 meta keywords
0 obsolete demo metadata/schema values
```

Do not keep old Class6 demo SEO active alongside the new Core.

---

## 14. Sitemap / robots contract

Generated from Project State + environment, not static accidental hardcoding.

For Release A, while HOME is the only real route:

- sitemap contains `/` only when published and indexable;
- no drafts, Studio, previews or technical routes;
- fully-qualified productive canonical URL only;
- robots policy cannot accidentally block production or expose preview indexing.

---

## 15. Regression / compatibility

Must preserve:

- existing RestaurantStore;
- Project State;
- undo/redo;
- autosave;
- export/import;
- Media Library;
- Motion;
- Product Engines;
- mobile/desktop;
- accessibility baseline.

Projects with no `seo` state must degrade/migrate through defaults.

Known pre-existing Motion/Class24 governance failures must remain documented separately. Release A must not hide, weaken or opportunistically fix them unless they are proven regressions caused by SEO/GEO work.

---

## 16. Release A minimum tests

At minimum:

```text
01 Source Data → deterministic AUTO title
02 city change removes stale location
03 incomplete data → graceful fallback
04 richer data beats generic fallback
05 canonical meta-description full composition
06 canonical meta-description reduced composition
07 CTA only if real capability exists
08 CUSTOM survives source changes
09 reset to AUTO recalculates
10 phone/email never enter title/H1
11 generic metadata does not receive false quality PASS
12 Spain-first: no operational English mode
13 raw productive HTML contains title/meta/canonical/robots/OG
14 preview is noindex
15 production index policy does not inherit preview noindex
16 productive canonical never uses Vercel preview
17 JSON-LD is valid Restaurant graph and uses real data
18 no fake ratings/reviews/hours/coordinates
19 sitemap contains only valid productive HOME when applicable
20 robots policy derives from environment/config
21 no duplicate legacy metadata/schema
22 meta keywords absent
23 export/import and persistence preserved
24 existing Studio/Motion regression evidence preserved
```

---

## 17. Release A Definition of Done

Release A is not complete until:

1. Spain-first contract is enforced;
2. Senior SEO Decision Engine is deterministic and explainable;
3. canonical meta-description composition is implemented;
4. generic-but-present metadata no longer receives false PASS;
5. future work is `DEFERRED`, not mixed with active blockers;
6. productive SEO exists in initial HTML output;
7. schema/canonical/robots/sitemap are production-safe;
8. legacy demo SEO is no longer simultaneously active;
9. relevant automated tests pass;
10. known baseline failures are separated from regressions;
11. Vercel preview is Ready;
12. human visual review is completed;
13. PR remains unmerged until explicit approval.

---

## 18. Collaboration rule while Astra is active

To avoid branch chaos:

- one active implementation branch: `feat/seo-geo-foundation`;
- one active PR: #44;
- no parallel feature branch for Release A;
- documentation-only changes from ChatGPT should be additive and avoid modifying Astra's active code files;
- Astra must pull/rebase the latest remote branch state before its next push if the branch HEAD has advanced;
- implementation changes remain Astra's responsibility during its active coding loop;
- ChatGPT may audit GitHub, refine contracts, tests and acceptance criteria, but should not concurrently edit the same implementation files.

**DO NOT MERGE WITHOUT HUMAN VISUAL APPROVAL.**
